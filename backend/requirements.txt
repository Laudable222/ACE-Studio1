fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
# pydantic-settings eagerly imports python-dotenv (its DotEnvSettingsSource) at package import;
# without it the whole app fails with ModuleNotFoundError: No module named 'dotenv'.
python-dotenv>=1.0
sqlalchemy>=2.0
alembic>=1.13
requests>=2.31
pandas>=2.2
python-multipart>=0.0.9
pypdf>=4.2
cryptography>=42.0
# pandas DataFrame.style (used by the vendored engine) needs jinja2 — without it, the
# brain engine import fails and every module that touches it (session, data, settings, …)
# is skipped, 404-ing the whole app.
jinja2>=3.1
# LLM SDKs (openai client also covers DeepSeek/Groq/OpenRouter/Together/xAI/Mistral)
openai>=1.30
anthropic>=0.34
google-genai>=0.3
