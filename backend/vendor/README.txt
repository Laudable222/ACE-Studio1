Vendored copies of the ACE engine (from studio/), so the v2 backend is self-contained
and does not depend on the studio/ folder. Do not edit here; if the engine changes,
re-copy. Added to sys.path by app/brain/engine.py.
